plugins {
    id 'java-library'
}

dependencies {
    // 버전은 BOM으로 가져오게 할 거라 명시 안 함
    api 'org.springframework.boot:spring-boot-starter-actuator'
    api 'io.micrometer:micrometer-registry-prometheus'
}